#!/bin/bash

set -x

{{ SCRIPTDIR }}/reset-appliance.sh

# Cleanup
ssh -q {{ NOCHECKSSH }} $RTARGET sudo su - student$stdid -c \'rm -rf {{ STUDDIR }}/student$stdid/.??* {{ STUDDIR }}/student$stdid/*\'

exit 0
